---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: tuned-client
  namespace: demo
spec:
  gatewayClassName: eg
  listeners:
    - name: http
      port: 80
      protocol: HTTP
      allowedRoutes:
        namespaces:
          from: Same
