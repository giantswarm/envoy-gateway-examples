---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: patched
  namespace: demo
spec:
  parentRefs:
    - name: patched
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: helloworld
          port: 8080
